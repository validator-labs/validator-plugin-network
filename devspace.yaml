name: validator-plugin-network
version: v2beta1

vars:
  IMAGE: quay.io/spectrocloud-labs/validator-plugin-network

images:
  validator-plugin-network:
    image: ${IMAGE}
    tags:
    - dev
    createPullSecret: false
    dockerfile: ./Dockerfile.devspace
    rebuildStrategy: ignoreContextChanges
    docker: {}

deployments:
  validator-plugin-network:
    namespace: validator-plugin-network-system
    kubectl:
      kustomize: true
      manifests:
      - config/devspace

dev:
  validator-plugin-network:
    imageSelector: ${IMAGE}
    terminal:
      enabled: true
      disableReplace: true
      workDir: /workspace
      command: ./devspace-start.sh
    ports:
    - port: "2343"
    sync:
    - path: .:/workspace
      excludePaths:
        - '**'
        - '!/pkg'
        - '!/api'
        - '!/cmd'
        - '!/config'
        - '!/internal'
        - '!/devspace-start.sh'
        - '!/hack'
        - '!/go.mod'
        - '!/go.sum'
        - '!/main.go'
        - 'Makefile'
